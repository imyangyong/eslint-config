{
  "name": "@imyangyong/eslint-config-monorepo",
  "version": "0.4.4",
  "private": true,
  "license": "MIT",
  "author": "Angus Yang",
  "scripts": {
    "lint": "eslint .",
    "test": "pnpm -r run test",
    "prepare": "pnpm -r run stub",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag 'release%s' && pnpm -r publish --access public"
  },
  "devDependencies": {
    "@imyangyong/eslint-config": "*",
    "bumpp": "^8.2.1",
    "eslint": "^8.28.0",
    "eslint-plugin-imyangyong": "workspace:*",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.3"
  }
}
