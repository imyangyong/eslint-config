{
  "name": "@imyangyong/eslint-config-monorepo",
  "version": "0.6.1",
  "private": true,
  "license": "MIT",
  "author": "Angus Yang",
  "scripts": {
    "lint": "eslint .",
    "test": "pnpm -r run test",
    "prepare": "pnpm -r run stub",
    "build": "pnpm -r run build",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag 'release%s'"
  },
  "devDependencies": {
    "@imyangyong/eslint-config": "*",
    "bumpp": "^8.2.1",
    "eslint": "^8.33.0",
    "eslint-plugin-imyangyong": "workspace:*",
    "rimraf": "^4.1.2",
    "typescript": "^4.9.4"
  }
}
